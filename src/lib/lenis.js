/* =========================================================
   LENIS CONFIG
   Project: Ansh Portfolio
   ========================================================= */

import Lenis from "lenis";

const lenis = new Lenis({
  autoRaf: true,
  smoothWheel: true,
  duration: 1.2,
});

export default lenis;